name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Build the Docker image
      uses: actions/checkout@v3
          
    - name: Docker Info
      run: docker login -u ${{ secrets.USER }} -p ${{ secrets.PASS }}
      
    - name: Docker Build
      run: docker build -t ${{ secrets.USER }}/flask-api:latest .
      
    - name: Upload container to Docker Hub with Push
      run: docker push ${{ secrets.USER }}/flask-api:latest
